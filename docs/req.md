# 需求说明（自动化与全流程通畅）

## 阶段目标
- 降低人工操作：新增“一键生成全流程”按钮。
- 增加步骤导出/导入、Markdown 文件导入。
- 提供自动化打包命令（dist 压缩包）。

## 约束
- 不引入新依赖。
- 侧边栏内完成核心操作。

## 验收标准
- 一键生成 Prompt / 执行计划 / 校验计划 / 执行模拟 / 断言模板 / 统一输出。
- 支持导出 steps.json。
- 支持导入 Markdown 文件并解析。
- `npm run package:ext` 输出 zip 包。
