# 需求说明（产品化完善：配置/兼容/交互/采集增强）

## 阶段目标
- 提升兼容性与交互体验，补齐可用性与配置能力。
- 增强采集：新增 change/scroll 事件、CSS 选择器回退。
- 增加配置面板：录制开关、事件开关、节流/防抖参数。
- 增加基础网络采集（仅记录请求元信息）。

## 约束
- 不引入新依赖。
- 保持现有消息结构。

## 验收标准
- 侧边栏可配置：启用/禁用录制、事件类型、节流/防抖。
- 录制在 Shadow DOM/复杂节点场景下至少有 CSS 选择器回退。
- 网络采集可开启，记录 URL/方法/状态码（不含敏感 body）。
