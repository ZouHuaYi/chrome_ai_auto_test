# 需求说明（Prompt 模板增强）

## 阶段目标
- 增强 Prompt 模板，加入测试目标、断言点占位、失败重试策略。
- 预留 LLM API 接口位置（仅占位，不接入）。

## 约束
- 不引入新依赖。
- 保持现有输出入口（侧边栏“生成 Prompt”）。

## 验收标准
- Prompt 输出包含：目标说明、步骤、断言占位、失败重试策略。
- 侧边栏按钮生成的 Prompt 可直接复制。
