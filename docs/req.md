# 需求说明（第二阶段：Prompt 组装器骨架）

## 阶段目标
- 新增 Prompt 组装器模块，将录制步骤 + 文档结构输出为统一 Prompt 文本。
- 侧边栏增加“生成 Prompt”功能，展示最终 Prompt。

## 约束
- 不引入新依赖。
- Prompt 输出保持可读，便于后续接 LLM。

## 验收标准
- 点击“生成 Prompt”可得到文本结果。
- Prompt 包含：步骤摘要 + 文档结构 JSON。
