# 需求说明（第二阶段：Markdown 文档解析模块）

## 阶段目标
- 新增 Markdown 文档解析能力。
- 将 .md 文档解析为结构化 JSON（标题层级 + 段落 + 列表）。

## 约束
- 先实现基础结构化输出，不涉及向量化或检索。
- 作为独立模块，不影响现有录制链路。

## 验收标准
- 输入 Markdown 字符串或文件内容，输出结构化 JSON。
- 兼容标题（# ~ ######）、无序列表（-/*/+）、有序列表（1./2.）。
